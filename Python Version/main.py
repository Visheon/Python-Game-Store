# ============================================
# START
# ============================================
#
# Entry point for the Game Store Rental Management System.
#
# This must be run inside a Jupyter environment (Jupyter Notebook,
# JupyterLab, or Google Colab) because the UI is built with
# ipywidgets, which need a live Jupyter frontend to render.
#
# From a Jupyter notebook cell, in the same folder as this file:
#
#     import main
#
# or, from a terminal, launch a notebook/lab session and run this
# file with the IPython "%run" magic:
#
#     %run main.py
#
# Either way, the dashboard is displayed as soon as this module runs.

from dashboard import dashboard

dashboard()
